# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
build
.react-router
*.local

# Secrets — jamais commités, voir .env.example
.env
.env.*
!.env.example

# Workspace local de l'outil scrollcraft (scratch, pas un livrable du site)
/scrollcraft/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Réglages locaux de l'agent (hooks impeccable, chemins propres à la machine)
.claude/settings.local.json

# État d'exécution du serveur impeccable (fichier de session, pas un livrable)
.impeccable/live/
